name: Dropdown

on:
    workflow_dispatch:
      inputs:
        name:
          type: choice
          description: Who to greet
          options:
          - monalisa
          - cschleiden
        message:
          required: true
          default: "default"
        use-emoji:
          type: boolean
          description: Include 🎉🤣 emojis
        boolean:
          type: boolean
          description: True or False

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Send greeting
        run: |
         echo "${{ github.event.inputs.message }} ${{ fromJSON('["","💡"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"
         echo "${{ github.event.inputs.message }}"
         echo "${{ github.event.inputs.name }}"
         echo "boolean is ${{ github.event.inputs.boolean }}"
         echo "${{ inputs.boolean }}"
         echo "${{ fromJSON('["","💡,💡,💡"]')[github.event.inputs.use-emoji == 'true'] }}"
         echo "${{ fromJSON('{"","💡","💡","💡",""}')[github.event.inputs.use-emoji == 'true'] }}" | jq